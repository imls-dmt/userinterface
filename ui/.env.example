# Copy to .env.local for local development overrides.
# Vue CLI embeds VUE_APP_* variables into the bundle at build time.

# Leave empty to use relative URLs (recommended when API and UI share an origin
# behind the nginx proxy in docker-compose).
# Set to a full URL when the API is on a different origin.
VUE_APP_API_BASE=
